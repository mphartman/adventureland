spring:
  datasource:
    url: jdbc:h2:mem:ebdb;MODE=MySQL
    username: sa
    password:

flyway:
  url: "jdbc:h2:mem:ebdb;MODE=MySQL;INIT=RUNSCRIPT FROM 'classpath:flyway_init.sql'"
  user: sa
  password:
  baseline-on-migrate: true